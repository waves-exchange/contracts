let priceAssetDecimals = claimedAssetId58.fromBase58String().assetInfo().value().decimals
let priceAssetBalanceEightDecimals = priceAssetBalance.fraction(scale8, pow(10, 0, priceAssetDecimals, 0, 0, DOWN))